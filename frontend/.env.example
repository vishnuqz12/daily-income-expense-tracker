# Leave blank when the frontend and FastAPI backend are hosted on the same HTTPS origin.
# Example when the API is hosted separately:
# VITE_API_URL=https://your-api.example.com
VITE_API_URL=https://daily-expense-api-jf5o.onrender.com
